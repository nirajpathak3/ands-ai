# Core runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# For the full set incl. LLM providers / data stores, install the extras:
#   pip install -e ".[llm,data,dev]"
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
langgraph>=0.2
langchain-core>=0.3
httpx>=0.27
